PROCEDURE Main()

   LOCAL cNome := "Patricia Oliveira"
   LOCAL cCidade := "São Paulo"

   CLS

   ? "==========================="
   ? "   FICHA DE APRESENTAÇÃO"
   ? "==========================="

   ? "Nome   :", cNome
   ? "Cidade :", cCidade
   ? "Curso  : Harbour/ADVPL"

   ? ""
   ? "==========================="
   ? "Nome   :", cNome
   ? "Cidade :", cCidade
   ? "Curso  : Harbour/ADVPL"
   ? "==========================="

   WAIT "Pressione qualquer tecla para sair..."

RETURN